# Test Report: IM App 微信化改造 + 附件项目归档（v1.1，S4 验证）

- **Change-ID**: 20260829-im-app-v2 · **阶段**: S4 · **日期**: 2026-08-29
- **验证结论**: 自动化验证全绿（W1 e2e 13/13、W2 7/7、W3 8/8、W4 7/7、全链 9/9）；**真机安装验收 owner GO（2026-08-29 终验通过）**

## 验证记录

### W1 · hooks 抽取（桌面零回归）
| 验证项 | 结果 |
| ------ | ---- |
| b2-im-notify-chain.mjs（打新构建） | ✅ 13/13 PASS |
| 桌面/手机 headless 双端走查 | ✅ 6/6（渲染/侧边栏/未跳登录） |
| F5 三不变量 | ✅ latestRef 回调 / selectedId 走 ref / setter 直传 |

### W2 · 移动端微信化 UI（已上线）
| 验证项 | 结果 |
| ------ | ---- |
| 会话列表（标题栏/发起聊天/空态） | ✅ 4/4 + 截图走查无问题 |
| 聊天视图（返回/输入栏/+按钮） | ✅ |
| 成员抽屉（成员/群主徽标/遮罩关闭） | ✅；修复：2 人群误标「单聊」→ 按 type==='GROUP' 判定 |
| 我方气泡（微信绿右侧无头像） | ✅（走查初报异常为测试脚本注入缺 id 字段，复验确认组件正确） |
| + 面板（相册/拍照/文件三格） | ✅ |
| emoji 面板（50 常用） | ✅ |
| 消息发送渲染 + data-mid 长按目标 | ✅ |
| 安全区 viewport-fit + env(safe-area-inset-*) | ✅ 代码层（真机走查项） |

### W3 · 附件上传 + 强制项目关联（已上线）
| 验证项 | 结果 |
| ------ | ---- |
| 会话未关联项目也可上传（强制选项目） | ✅ |
| UploadPicker 单弹层（搜索/项目列表/目录树/归档提示） | ✅ |
| 上传→消息 fileMeta 归属快照 | ✅ `{projectId, projectName, catalogName}` 落库 |
| 文件卡片「📁 项目/目录」归属行 | ✅ |
| 测试文件清理 | ✅ 经 GET /api/files 列表端点定位删除 |
| 权限灰显（OWNER/ADMIN 放行其余置灰） | ✅ 代码层（接口层 requireCan 与 PC 同链路） |

### W4 · PC 文件移动（已上线）
| 验证项 | 结果 |
| ------ | ---- |
| PATCH move：计划外文件移动成功 | ✅ |
| storagePath 指向新目录 + 下载可用 | ✅ |
| move 回原目录（幂等） | ✅ |
| 跨项目 move → 400「仅支持项目内移动」 | ✅ |
| GET /api/files 计划外列表（前缀匹配） | ✅ |
| prisma MOVE 枚举迁移 | ✅ migrate deploy 无待应用 |

### W5 · 出包与分发
| 验证项 | 结果 |
| ------ | ---- |
| v1.1 APK（versionCode 2 / 4.5M / v2+v3 签名） | ✅ SHA-256 ede2898f… |
| /downloads/pm-chat-1.1.0.apk 200 + MIME | ✅ |
| 下载页版本/SHA 更新 | ✅ |
| verify-im-app.mjs 全链 | ✅ 9/9 |

## 已知边界（真机验收关注点）

1. 长按菜单/滚动 pill/键盘三态：headless 已验证元素与事件绑定，触屏手感需真机确认
2. 历史上拉加载：游标分页已通（>50 条会话），深翻页加载时滚动位置补偿已实现
3. 老 APK（v1.0）用户无版本检查：发布 = 通知全员重扫（回滚路径 = git revert 重建部署）
4. 群头像：members[].avatar 有照片则显示照片，当前员工均无头像照片 → 显示首字母圈（可后续在 PC 上传头像）

---
> 闸门：自动化验证全绿 → 进入 S5。**GO/NO-GO 由 owner 真机扫码升级验收决定**。
